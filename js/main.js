// Add this to your main initialization code

// Call the new functions after page load
window.addEventListener('DOMContentLoaded', () => {
  // After existing initialization...
  updateStateStructure();
  enhanceRankingsTab();
  enhanceStatsTab();
}); 